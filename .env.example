# URL interna de PocketBase (dentro de Docker usa el nombre del servicio)
# En desarrollo local: http://localhost:8090
PB_URL=http://pocketbase:8090

# URL pública de PocketBase (la que ve el navegador del cliente para cargar imágenes)
# En producción: el subdominio expuesto por Traefik
PUBLIC_PB_URL=https://cms.petalosmary.lsoft.me
